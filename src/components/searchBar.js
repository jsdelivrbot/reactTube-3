import React, { Component } from 'react';

class SearchBar extends Component {
    render() {
        return (
            <div>
                I'm a search bar!'
            </div>
        )
    }
}

export default SearchBar;